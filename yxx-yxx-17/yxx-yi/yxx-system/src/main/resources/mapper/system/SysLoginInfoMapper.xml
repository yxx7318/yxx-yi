<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yxx.system.mapper.SysLoginInfoMapper">

    <resultMap type="SysLoginInfo" id="SysLoginInfoResult">
        <id     property="infoId"        column="info_id"           />
        <result property="userName"      column="user_name"         />
        <result property="status"        column="status"            />
        <result property="ipaddr"        column="ipaddr"            />
        <result property="loginLocation" column="login_location"    />
        <result property="browser"       column="browser"           />
        <result property="os"            column="os"                />
        <result property="msg"           column="msg"               />
        <result property="loginTime"     column="login_time"        />
        <result property="createById"    column="create_by_id"      />
        <result property="createByName"  column="create_by_name"    />
        <result property="createTime"    column="create_time"       />
        <result property="updateById"    column="update_by_id"      />
        <result property="updateByName"  column="update_by_name"    />
        <result property="updateTime"    column="update_time"       />
        <result property="remark"        column="remark"            />
    </resultMap>

    <insert id="insertLoginInfo" parameterType="SysLoginInfo">
        insert into sys_login_info (user_name, status, ipaddr, login_location, browser, os, msg, login_time,
                                    create_by_id, create_by_name, create_time, remark)
        values (#{userName}, #{status}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{msg}, sysdate(),
                #{createById}, #{createByName}, #{createTime}, ifnull(#{remark}, ''))
    </insert>

    <select id="selectLoginInfoList" parameterType="SysLoginInfo" resultMap="SysLoginInfoResult">
        select info_id, user_name, ipaddr, login_location, browser, os, status, msg, login_time,
               create_by_id, create_by_name, create_time, update_by_id, update_by_name, update_time, remark from sys_login_info
        <where>
            <if test="ipaddr != null and ipaddr != ''">
                AND ipaddr like concat('%', #{ipaddr}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="userName != null and userName != ''">
                AND user_name like concat('%', #{userName}, '%')
            </if>
            <if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
                AND login_time &gt;= #{params.beginTime}
            </if>
            <if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
                AND login_time &lt;= #{params.endTime}
            </if>
        </where>
        order by info_id desc
    </select>

    <delete id="deleteLoginInfoByIds" parameterType="Long">
         delete from sys_login_info where info_id in
         <foreach collection="array" item="infoId" open="(" separator="," close=")">
             #{infoId}
        </foreach>
     </delete>

    <update id="cleanLoginInfo">
        truncate table sys_login_info
    </update>

</mapper>